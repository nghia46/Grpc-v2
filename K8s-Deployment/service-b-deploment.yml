apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-b-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-b-app
  template:
    metadata:
      labels:
        app: service-b-app
    spec:
      containers:
      - name: service-b
        image: nghialoe/service-b:latest
        ports:
          - containerPort: 5005  # gRPC (HTTP/2)\
          - containerPort: 8080  # gRPC (HTTP/2)
      #   volumeMounts:
      #   - name: config-volume
      #     mountPath: /app/config/appsettings.json
      #     subPath: appsettings.json
      # volumes:
      # - name: config-volume
      #   configMap:
      #     name: app-config
---
# This is the service that will be exposed to the outside world
apiVersion: v1
kind: Service
metadata:
  name: service-b-api-lb
spec:
  selector:
    app: service-b-app
  ports:
    - protocol: TCP
      port: 81
      targetPort: 8080
  type: LoadBalancer
---
# This is the service for gRPC
apiVersion: v1
kind: Service
metadata:
  name: service-b-lb
spec:
  selector:
    app: service-b-app
  ports:
    - protocol: TCP
      port: 50
      targetPort: 5005
  type: LoadBalancer
---